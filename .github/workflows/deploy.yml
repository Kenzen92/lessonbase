name: Deploy to VPS

on:
  workflow_run:
    workflows: ["Test"]  # Trigger on workflow run
    types: [completed]
    branches: [main]  # Only deploy from main branch


env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  FRONTEND_IMAGE: ${{ secrets.DOCKER_USERNAME }}/lessonbase-frontend
  BACKEND_IMAGE: ${{ secrets.DOCKER_USERNAME }}/lessonbase-backend

jobs:
  build-and-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    environment: production
    # Only deploy if tests succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata for backend
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.BACKEND_IMAGE }}
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=registry,ref=${{ env.BACKEND_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.BACKEND_IMAGE }}:buildcache,mode=max

      - name: Extract metadata for frontend
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.FRONTEND_IMAGE }}
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          cache-from: type=registry,ref=${{ env.FRONTEND_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.FRONTEND_IMAGE }}:buildcache,mode=max
          build-args: |
            VITE_REACT_APP_API_URL=https://api.teach.jkenny.tech
            VITE_REACT_APP_WEBSOCKET_URL=wss://api.teach.jkenny.tech/ws
            VITE_GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}

  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Prepare deployment files
        run: |
          # Create base64 encoded versions of files for transfer
          echo "COMPOSE_B64<<EOF" >> $GITHUB_ENV
          base64 -w 0 deploy/docker/docker-compose.vps.yml >> $GITHUB_ENV
          echo "" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Copy docker-compose to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            echo "📋 Updating docker-compose.yml..."
            echo "${{ env.COMPOSE_B64 }}" | base64 -d > /opt/lessonbase/docker-compose.yml
            echo "✅ docker-compose.yml updated"

      - name: Write secrets to .env on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            echo "🔑 Updating .env with secrets..."
            ENV_FILE=/opt/lessonbase/.env

            update_env() {
              local key="$1" val="$2"
              [ -z "$val" ] && return  # skip if secret not set in GitHub
              if grep -q "^${key}=" "$ENV_FILE" 2>/dev/null; then
                sed -i "s|^${key}=.*|${key}=${val}|" "$ENV_FILE"
              else
                echo "${key}=${val}" >> "$ENV_FILE"
              fi
            }

            update_env SECRET_KEY           "${{ secrets.SECRET_KEY }}"
            update_env DJANGO_SECRET_KEY    "${{ secrets.SECRET_KEY }}"
            update_env POSTGRES_PASSWORD    "${{ secrets.POSTGRES_PASSWORD }}"
            update_env REDIS_PASSWORD       "${{ secrets.REDIS_PASSWORD }}"
            update_env GOOGLE_CLIENT_SECRET "${{ secrets.GOOGLE_CLIENT_SECRET }}"
            update_env EMAIL_HOST_PASSWORD  "${{ secrets.EMAIL_HOST_PASSWORD }}"
            update_env R2_ACCESS_KEY_ID     "${{ secrets.R2_ACCESS_KEY_ID }}"
            update_env R2_ACCESS_SECRET     "${{ secrets.R2_ACCESS_SECRET }}"
            update_env R2_S3_API            "${{ secrets.R2_S3_API }}"
            update_env R2_BUCKET_NAME       "${{ secrets.R2_BUCKET_NAME }}"
            echo "✅ .env updated"

      - name: Sync nginx configs to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            echo "📋 Updating nginx configs from git..."
            cd /tmp
            rm -rf deploy-temp
            git clone --depth 1 --branch main --single-branch https://github.com/Kenzen92/kennysolutions.git deploy-temp
            cp -r deploy-temp/nginx/* /opt/lessonbase/nginx/
            rm -rf deploy-temp
            echo "✅ Nginx configs updated"

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script_stop: true
          script: |
            set -e

            echo "🚀 Starting deployment..."
            cd /opt/lessonbase

            # Login to Docker Hub
            echo "🔐 Logging into Docker Hub..."
            echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

            # Pull latest images
            echo "📦 Pulling latest Docker images..."
            docker pull ${{ env.BACKEND_IMAGE }}:latest
            docker pull ${{ env.FRONTEND_IMAGE }}:latest

            # Run database migrations
            echo "🗄️ Running database migrations..."
            docker compose run --rm backend /app/migrate.sh || echo "⚠️ Migration failed or no migrations needed"

            # Restart services with zero downtime
            echo "♻️ Restarting services..."
            docker compose up -d --pull always --no-build backend frontend

            # Cleanup old images
            echo "🧹 Cleaning up old images..."
            docker image prune -af --filter "until=72h" || true

            # Health check
            echo "🏥 Running health checks..."
            sleep 10

            # Check backend health
            if curl -f https://api.teach.jkenny.tech/admin/login/ > /dev/null 2>&1; then
              echo "✅ Backend is healthy"
            else
              echo "❌ Backend health check failed"
              exit 1
            fi

            # Check frontend health
            if curl -f https://teach.jkenny.tech > /dev/null 2>&1; then
              echo "✅ Frontend is healthy"
            else
              echo "❌ Frontend health check failed"
              exit 1
            fi

            echo "✨ Deployment completed successfully!"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Deployment failed! Check the logs above for details."
          # You can add Slack/Discord/Email notification here

  cleanup:
    name: Cleanup
    runs-on: ubuntu-latest
    needs: deploy
    if: always()

    steps:
      - name: Logout from Docker Hub
        run: docker logout || true
